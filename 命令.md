## tar 命令

### tar -zxvf‌

tar 命令是 Linux 下用于‌解压‌ .tar.gz 格式压缩包的常用命令组合。

-z：解压时使用 gzip 压缩\
-x：解压文件\
-v：显示解压过程\
-f：指定压缩包文件名，必须放最后面

```shell
tar -zxvf 文件名.tar.gz
```

指定解压到特定目录，使用 ‌-C‌ 参数，例如：

```shell
tar -zxvf 文件名.tar.gz -C 目录名
```

### tail -n 100 -f 文件名

tail 命令是 Linux 下用于‌查看‌文件内容的常用命令组合。

-n：显示最近 100 行\
-f：实时显示文件内容

```shell
tail -n 100 -f 文件名
```

## chmod 命令

### 执行 chmod +x

通常是为了赋予启动脚本或二进制文件‌执行权限‌

```shell
chmod +x 文件名
# 或者单独赋予启动/停止脚本权限
chmod +x startup.sh
chmod +x shutdown.sh
```
